<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        Phase C.11 (2026-05-30): the DispatcherServlet child context's bean
        graph is now defined in
        at.ac.meduniwien.ophthalmology.libreclinica.webmvc.WebMvcConfig.
        This file is kept as a one-line stub so the legacy `pages`
        DispatcherServlet (declared in web.xml without a contextConfigLocation
        init-param) finds its default config path `/WEB-INF/pages-servlet.xml`.

        <context:annotation-config/> activates ConfigurationClassPostProcessor
        for the @Configuration class below — the DispatcherServlet's child
        context doesn't inherit the root context's annotation processors,
        so we register them locally.
    -->
    <context:annotation-config/>

    <bean class="at.ac.meduniwien.ophthalmology.libreclinica.webmvc.WebMvcConfig"/>

</beans>
