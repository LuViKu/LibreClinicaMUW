<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>at.ac.meduniwien.ophthalmology.libreclinica</groupId>
	<artifactId>LibreClinica</artifactId>
	<packaging>pom</packaging>
	<version>1.4.0rc1-muw</version>
	<name>LibreClinica MUW Ophthalmology</name>
	<description>Electronic Data Capture (EDC) for clinical trials — institutional build of LibreClinica for the Department of Ophthalmology and Optometry, Medical University of Vienna.</description>
	<organization>
		<name>Department of Ophthalmology and Optometry, Medical University of Vienna</name>
		<url>https://www.meduniwien.ac.at/web/en/about-us/organisation/university-clinical-departments/clinical-department-of-ophthalmology-and-optometry/</url>
	</organization>

	<modules>
		<module>core</module>
		<module>odm</module>
		<module>docs</module>
		<module>web</module>
	</modules>

	<properties>
		<!-- Default configuration activation -->
		<config.id>default</config.id>

		<!-- Phase 0 (2026-05-28): unit tests run by default. Integration tests
		     that require a live PostgreSQL connection are excluded by surefire
		     configuration below; they will be replaced with Testcontainers-based
		     integration tests in a later Phase 0 iteration. Override with
		     -DskipTests=true for fast local builds. -->
		<skipTests>false</skipTests>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<!-- Project's source control URL -->
		<git.url>https://github.com/reliatec-gmbh/LibreClinica.git</git.url>

		<!-- Config file - required by liquibase plugin -->
		<config.file>src/main/resources/datainfo.properties</config.file>

		<!-- Library versions. Phase B.4 (2026-05-29) crossed the jakarta cliff:
		     Spring 5.3 → 6.1, Spring Security 5.8 → 6.3, javax.* → jakarta.*,
		     hibernate-core swapped to the hibernate-core-jakarta artifact (still
		     5.6.15 — Hibernate 6.4 in B.5). Phase C.1 (2026-05-29) imports
		     spring-boot-dependencies 3.5 BOM and aligns Spring 6.1 → 6.2,
		     Spring Security 6.3 → 6.4 to match Boot's managed transitives —
		     mixing 6.1 + 6.2 jars caused CGLIB ClassLoaderAwareGeneratorStrategy
		     signature mismatches at runtime. -->
		<spring-boot.version>3.5.0</spring-boot.version>
		<spring.version>6.2.6</spring.version>
		<spring.security.version>6.4.4</spring.security.version>
		<db.test>localhost</db.test>
		<jackson.version>2.17.3</jackson.version>
		<saxon.version>8.7</saxon.version>
		<!-- Phase B.5 (2026-05-29): Hibernate 5.6.15 → 6.4.10. groupId moves
		     to org.hibernate.orm; the -jakarta artifact-id suffix goes away
		     (6.x is jakarta-native). L2 cache via hibernate-jcache + ehcache 3
		     (JSR-107). Spring 6's org.springframework.orm.hibernate5 package is
		     retained on classpath and used for LocalSessionFactoryBean +
		     HibernateTransactionManager + HibernateTemplate — Hibernate 6
		     preserves the Configuration / Session / SessionFactory API surface
		     these depend on. -->
		<hibernate.version>6.4.10.Final</hibernate.version>
		<ehcache.version>3.10.8</ehcache.version>
		<jakarta.xml.bind.version>4.0.2</jakarta.xml.bind.version>
		<jaxb.runtime.version>4.0.5</jaxb.runtime.version>
		<jakarta.servlet.version>6.0.0</jakarta.servlet.version>
		<jakarta.servlet.jsp.version>3.1.1</jakarta.servlet.jsp.version>
		<jakarta.jstl.version>3.0.0</jakarta.jstl.version>
		<jakarta.ws.rs.version>3.1.0</jakarta.ws.rs.version>
		<jakarta.mail.version>2.1.3</jakarta.mail.version>
		<jakarta.activation.version>2.1.3</jakarta.activation.version>
		<jakarta.annotation.version>2.1.1</jakarta.annotation.version>
		<!-- Phase C.14 cliff (2026-05-30): bumped 1.4.14 → 1.5.13 to match
		     Spring Boot 3.5's managed version. Boot's LoggingApplicationListener
		     loads ch.qos.logback.core.util.StatusPrinter2 (added in 1.5+)
		     before checking the LoggingSystem=none escape hatch. -->
		<logback.version>1.5.13</logback.version>
		<slf4j.version>2.0.16</slf4j.version>
		<!-- Selenium 4.x (test scope) drives the smoke-tests profile.
		     4.20+ supports Java 21 + the Selenium-managed driver download. -->
		<selenium.version>4.25.0</selenium.version>
	</properties>
	<profiles>
		<profile>
			<!-- Enables Phase 0 integration tests (DAO + rule-service tests that
			     extend HibernateOcDbTestCase / OcDbTestCase and require a live
			     PostgreSQL on localhost:5432, database 'openclinica-TEST', user
			     'clinica' / password 'clinica' per core/src/test/resources/datainfo.properties).
			     Activate with: mvn verify -P integration-tests
			     CI activates this profile in the integration-tests job which
			     spins up a postgres:14-alpine service container.
			     NOTE: this profile does NOT yet bootstrap the database schema;
			     test schema setup via Liquibase is pending work for Phase 0.2. -->
			<id>integration-tests</id>
			<build>
				<pluginManagement>
					<plugins>
						<plugin>
							<groupId>org.apache.maven.plugins</groupId>
							<artifactId>maven-surefire-plugin</artifactId>
							<configuration>
								<!-- combine.self="override" wipes the exclude list inherited
								     from the parent surefire pluginManagement config so the
								     DB-dependent tests actually run under this profile.
								     **/*SmokeIT.java is re-excluded explicitly because the
								     **/*IT.java include below otherwise scoops up our
								     Selenium browser-smokes — those need a running stack
								     + WebDriver hub which CI doesn't provide. They live
								     in the smoke-tests profile (see below). -->
								<excludes combine.self="override">
									<exclude>**/*SmokeIT.java</exclude>
								</excludes>
								<!-- Phase 0 backlog ITs follow the failsafe naming
								     convention (*IT.java), which surefire's default
								     includes do not pick up. Explicit include keeps
								     them in the integration-tests profile alongside
								     the legacy *Test.java suites. The default includes
								     are repeated so the profile keeps them too. -->
								<includes combine.self="override">
									<include>**/*Test.java</include>
									<include>**/*Tests.java</include>
									<include>**/Test*.java</include>
									<include>**/*IT.java</include>
								</includes>
								<!-- Phase 0 IT-investigation gate (see EntityDAO):
								     keep the real SQLException visible via
								     getFailureDetails() instead of overwriting
								     it with the downstream empty exception. -->
								<systemPropertyVariables>
									<oc.dao.preserveFirstSqlException>true</oc.dao.preserveFirstSqlException>
								</systemPropertyVariables>
							</configuration>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
		</profile>
		<profile>
			<!-- Enables Selenium browser-smoke tests (*SmokeIT.java in
			     web/src/test/java/). Tests run a Chrome WebDriver against
			     a manually-started app at smoke.base.url (default
			     http://localhost:8080/LibreClinica/). Activate with:
			       docker compose up   # start the LibreClinica stack
			       mvn -pl web -P smoke-tests test
			     CI does NOT run this profile yet (would need Docker-in-
			     Docker for the WebDriver container). Operator runs locally
			     to gate cohort merges per phase-b4-jmesa-replacement.md.
			     See docs/development/smoke-testing.md.

			     Phase B.4 jmesa PR 4a smoke-test slot: AuditUserActivitySmokeIT. -->
			<id>smoke-tests</id>
			<properties>
				<smoke.base.url>http://localhost:8080/LibreClinica/</smoke.base.url>
				<smoke.headless>true</smoke.headless>
			</properties>
			<build>
				<pluginManagement>
					<plugins>
						<plugin>
							<groupId>org.apache.maven.plugins</groupId>
							<artifactId>maven-surefire-plugin</artifactId>
							<configuration>
								<excludes combine.self="override"/>
								<includes combine.self="override">
									<include>**/*SmokeIT.java</include>
								</includes>
								<systemPropertyVariables>
									<smoke.base.url>${smoke.base.url}</smoke.base.url>
									<smoke.headless>${smoke.headless}</smoke.headless>
								</systemPropertyVariables>
							</configuration>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
		</profile>
		<profile>
			<id>codeship</id>
			<properties>
				<db.test>107.170.116.150</db.test>
			</properties>
		</profile>
		<profile>
			<!-- This profile is not compatible with Maven v2. Use it with Maven v3 only. -->
			<id>m2eclipse-config</id>
			<activation>
				<property>
					<name>useJRebel</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<pluginManagement>
					<plugins>
						<plugin>
							<groupId>org.eclipse.m2e</groupId>
							<artifactId>lifecycle-mapping</artifactId>
							<version>1.0.0</version>
							<configuration>
								<lifecycleMappingMetadata>
									<pluginExecutions>
										<pluginExecution>
											<pluginExecutionFilter>
												<groupId>org.zeroturnaround</groupId>
												<artifactId>jrebel-maven-plugin</artifactId>
												<versionRange>[1.0.0,)</versionRange>
												<goals>
													<goal>generate</goal>
												</goals>
											</pluginExecutionFilter>
											<action>
												<ignore />
											</action>
										</pluginExecution>
										<!-- <pluginExecution> <pluginExecutionFilter> <groupId>org.codehaus.mojo</groupId>
											<artifactId>buildnumber-maven-plugin</artifactId> <versionRange>[1.0.0,)</versionRange>
											<goals> <goal>create</goal> </goals> <configuration> <doUpdate>false</doUpdate>
											<getRevisionOnlyOnce>true</getRevisionOnlyOnce> </configuration> </pluginExecutionFilter>
											<action> <ignore /> </action> </pluginExecution> -->
									</pluginExecutions>
								</lifecycleMappingMetadata>
							</configuration>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
		</profile>
	</profiles>

	<dependencyManagement>
		<dependencies>
			<!-- Phase C.1: Spring Boot BOM. Imported FIRST so explicit
			     dependency entries below override Boot's defaults where
			     the legacy version pin diverges from Boot's recommendation.
			     Per-dep overrides (Spring 6.1.18, Security 6.3.6, Hibernate
			     6.4.10, Jackson 2.17.3, …) stay in place until each sub-phase
			     verifies the bumped version. -->
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${spring-boot.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>${project.groupId}.odm</groupId>
				<artifactId>LibreClinica-odm</artifactId>
				<version>2.2-muw</version>
			</dependency>
			<!-- Phase B.4 jmesa PR 9 (cohort 7): jmesa dependencyManagement
			     entry dropped. The 2.4.2-oc local-OpenClinica fork is no
			     longer needed by any module. -->
			<dependency>
				<!-- 2.2.3 -> 2.3.2: final 2.3.x release, API-compatible.
				     Jump to 2.5.x deferred to Phase D (Jakarta-compatible namespace). -->
				<groupId>org.quartz-scheduler</groupId>
				<artifactId>quartz</artifactId>
				<version>2.3.2</version>
			</dependency>
			<dependency>
				<groupId>log4jdbc</groupId>
				<artifactId>log4jdbc4</artifactId>
				<version>1.2</version>
			</dependency>
			<dependency>
				<!-- Selenium 4.x for the smoke-tests profile. Pulled as a
				     test-scope dep by web/pom.xml; not used in production. -->
				<groupId>org.seleniumhq.selenium</groupId>
				<artifactId>selenium-java</artifactId>
				<version>${selenium.version}</version>
				<scope>test</scope>
			</dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <!-- Phase B.4: commons-fileupload 1.x targets javax.servlet.
                 commons-fileupload2-jakarta-servlet6 is the jakarta-namespaced
                 successor. API surface differs (org.apache.commons.fileupload2.*);
                 callers are updated in the same PR. -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-fileupload2-core</artifactId>
                <version>2.0.0-M2</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-fileupload2-jakarta-servlet6</artifactId>
                <version>2.0.0-M2</version>
            </dependency>
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>2.16.1</version>
            </dependency>
			<dependency>
				<groupId>commons-beanutils</groupId>
				<artifactId>commons-beanutils</artifactId>
				<version>1.11.0</version>
			</dependency>
			<dependency>
				<groupId>commons-dbcp</groupId>
				<artifactId>commons-dbcp</artifactId>
				<version>1.4</version>
			</dependency>
			<!-- dbcp2 2.8 contains performance regression that have negative impact on big LC installation -->
			<!-- only move to new version if performance testing provides better results -->
			<!--
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-dbcp2</artifactId>
				<version>2.8.0</version>
			</dependency>
			-->
			<dependency>
				<groupId>commons-logging</groupId>
				<artifactId>commons-logging</artifactId>
				<version>1.0.4</version>
			</dependency>
			<dependency>
				<groupId>commons-logging</groupId>
				<artifactId>commons-logging-api</artifactId>
				<version>1.0.4</version>
			</dependency>
			<dependency>
				<!-- Final 2.x release; 3.x is a package rename (org.apache.commons.lang
				     -> org.apache.commons.lang3) and is deferred to Phase B. -->
				<groupId>commons-lang</groupId>
				<artifactId>commons-lang</artifactId>
				<version>2.6</version>
			</dependency>
			<dependency>
				<groupId>net.sf.saxon</groupId>
				<artifactId>saxon</artifactId>
				<version>${saxon.version}</version>
			</dependency>
			<dependency>
				<groupId>net.sf.saxon</groupId>
				<artifactId>saxon-dom</artifactId>
				<version>${saxon.version}</version>
			</dependency>
			<dependency>
				<groupId>commons-codec</groupId>
				<artifactId>commons-codec</artifactId>
				<version>1.17.1</version>
			</dependency>
			<dependency>
				<groupId>commons-validator</groupId>
				<artifactId>commons-validator</artifactId>
				<version>1.9.0</version>
			</dependency>
			<dependency>
				<!-- 3.2.1 -> 3.2.2: patches CVE-2015-7501 (insecure deserialization).
				     Migration to commons-collections4 (different package) is Phase B. -->
				<groupId>commons-collections</groupId>
				<artifactId>commons-collections</artifactId>
				<version>3.2.2</version>
			</dependency>
			<dependency>
				<groupId>commons-math</groupId>
				<artifactId>commons-math</artifactId>
				<version>1.1</version>
			</dependency>
			<dependency>
				<groupId>commons-digester</groupId>
				<artifactId>commons-digester</artifactId>
				<version>1.7</version>
				<scope>compile</scope>
				<exclusions>
					<exclusion>
						<artifactId>commons-collections</artifactId>
						<groupId>commons-collections</groupId>
					</exclusion>
					<exclusion>
						<artifactId>xml-apis</artifactId>
						<groupId>xml-apis</groupId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-core</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-web</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-beans</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-orm</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
	            <groupId>org.springframework</groupId>
	            <artifactId>spring-webmvc</artifactId>
	            <version>${spring.version}</version>
	        </dependency>
			<dependency>
	            <groupId>org.springframework</groupId>
	            <artifactId>spring-jcl</artifactId>
	            <version>${spring.version}</version>
	            <scope>runtime</scope>
	        </dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-test</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-ldap</artifactId>
				<version>${spring.security.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-taglibs</artifactId>
				<version>${spring.security.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-acl</artifactId>
				<version>${spring.security.version}</version>
			</dependency>
			<dependency>
			    <groupId>org.springframework.security</groupId>
			    <artifactId>spring-security-config</artifactId>
			    <version>${spring.security.version}</version>
			    <scope>runtime</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-oxm</artifactId>
				<version>${spring.version}</version>
				<scope>runtime</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-tx</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<!-- Phase B.4 cliff: spring-security-oauth 2.3.5 (javax-only, archived
			     upstream) removed; see core/pom.xml for rationale. -->

	       <dependency>
	           <groupId>org.springframework.security</groupId>
	           <artifactId>spring-security-web</artifactId>
	           <version>${spring.security.version}</version>
	       </dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-context</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework</groupId>
				<artifactId>spring-jdbc</artifactId>
				<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-core</artifactId>
				<version>${spring.security.version}</version>
			</dependency>
			<dependency>
				<!-- Direct dependency since Phase A.2 (Spring Security 5.8 bump):
				     OpenClinicaPasswordEncoder + SecurityManager + EnketoAPI +
				     PFormCache import org.springframework.security.crypto.*
				     (Hex, Utf8, PasswordEncoder). Was transitive before; declared
				     explicitly here to satisfy maven-dependency-plugin:analyze-only. -->
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-crypto</artifactId>
				<version>${spring.security.version}</version>
			</dependency>
			<dependency>
	    		<groupId>org.springframework</groupId>
	    		<artifactId>spring-context-support</artifactId>
	    		<version>${spring.version}</version>
			</dependency>
			<dependency>
				<groupId>org.springframework.ldap</groupId>
				<artifactId>spring-ldap-core</artifactId>
				<!-- make sure the version matches the one used by org.springframework.security:spring-security-ldap -->
				<version>2.3.2.RELEASE</version>
			</dependency>
			<!-- Phase B.5: net.sf.ehcache (2.x, javax-only) replaced by
			     org.ehcache:ehcache 3 + hibernate-jcache below.
			     net.sf.ehcache:ehcache-web (GzipFilter) was already removed
			     during the B.4 cliff. -->
			<dependency>
				<groupId>org.ehcache</groupId>
				<artifactId>ehcache</artifactId>
				<version>${ehcache.version}</version>
				<classifier>jakarta</classifier>
			</dependency>
	        <dependency>
	        	<groupId>jakarta.activation</groupId>
				<artifactId>jakarta.activation-api</artifactId>
				<version>${jakarta.activation.version}</version>
	        </dependency>
	        <dependency>
	        	<groupId>jakarta.annotation</groupId>
				<artifactId>jakarta.annotation-api</artifactId>
				<version>${jakarta.annotation.version}</version>
	        </dependency>
			<dependency>
			   <groupId>jakarta.persistence</groupId>
			   <artifactId>jakarta.persistence-api</artifactId>
			   <version>3.1.0</version>
			</dependency>
			<dependency>
				<groupId>jakarta.ws.rs</groupId>
				<artifactId>jakarta.ws.rs-api</artifactId>
				<version>${jakarta.ws.rs.version}</version>
			</dependency>
			<dependency>
				<groupId>jakarta.servlet.jsp</groupId>
				<artifactId>jakarta.servlet.jsp-api</artifactId>
				<version>${jakarta.servlet.jsp.version}</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>jakarta.servlet.jsp.jstl</groupId>
				<artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
				<version>${jakarta.jstl.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.web</groupId>
				<artifactId>jakarta.servlet.jsp.jstl</artifactId>
				<version>3.0.1</version>
			</dependency>
	        <dependency>
	            <groupId>jakarta.servlet</groupId>
	            <artifactId>jakarta.servlet-api</artifactId>
	            <version>${jakarta.servlet.version}</version>
	            <scope>provided</scope>
	        </dependency>
			<dependency>
				<groupId>jakarta.mail</groupId>
				<artifactId>jakarta.mail-api</artifactId>
				<version>${jakarta.mail.version}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.angus</groupId>
				<artifactId>angus-mail</artifactId>
				<version>2.0.3</version>
				<scope>runtime</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.xmlgraphics</groupId>
				<artifactId>fop</artifactId>
				<version>1.0</version>
			</dependency>
			<dependency>
				<groupId>xml-apis</groupId>
				<artifactId>xml-apis</artifactId>
				<!-- make sure the version matches the one used by the batik libs (e.g. org.apache.xmlgraphics:batik-dom) -->
				<version>1.3.04</version>
			</dependency>
			<!-- HIBERNATE — jakarta-namespaced artifact at 5.6.x.
			     B.5 bumps to Hibernate 6.4.x (jakarta-native; no separate
			     artifact suffix needed). Hibernate L2 cache (formerly
			     hibernate-ehcache) does not ship a -jakarta sibling at
			     5.6.x; re-wire via hibernate-jcache + ehcache 3 in B.5
			     once on Hibernate 6.4. -->
			<dependency>
				<groupId>org.hibernate.orm</groupId>
				<artifactId>hibernate-core</artifactId>
				<version>${hibernate.version}</version>
			</dependency>
			<dependency>
				<groupId>org.hibernate.orm</groupId>
				<artifactId>hibernate-jcache</artifactId>
				<version>${hibernate.version}</version>
			</dependency>
			<dependency>
	    		<groupId>com.mattbertolini</groupId>
	    		<artifactId>liquibase-slf4j</artifactId>
	    		<version>3.0.0</version>
	    		<scope>runtime</scope>
			</dependency>
			<!-- LIQUIBASE -->
			<dependency>
				<groupId>org.liquibase</groupId>
				<artifactId>liquibase-core</artifactId>
				<version>3.6.3</version>
			</dependency>
			<!-- LOGBACK -->
			<dependency>
				<groupId>ch.qos.logback</groupId>
				<artifactId>logback-core</artifactId>
				<version>${logback.version}</version>
			</dependency>
			<dependency>
				<groupId>ch.qos.logback</groupId>
				<artifactId>logback-classic</artifactId>
				<version>${logback.version}</version>
			</dependency>
			<!-- LOGBACK needs Janino when conditional statements are used (e.g. in logback.xml).
			     Phase C.1: bump 2.7.5 → 3.1.12 so janino's Location matches
			     commons-compiler 3.x that Boot 3.5's logback transitive pulls in. -->
			<dependency>
				<groupId>org.codehaus.janino</groupId>
				<artifactId>janino</artifactId>
				<version>3.1.12</version>
			</dependency>
			<dependency>
				<groupId>org.codehaus.janino</groupId>
				<artifactId>commons-compiler</artifactId>
				<version>3.1.12</version>
			</dependency>
			<dependency>
				<groupId>jakarta.xml.bind</groupId>
				<artifactId>jakarta.xml.bind-api</artifactId>
				<scope>compile</scope>
				<version>${jakarta.xml.bind.version}</version>
			</dependency>
			<dependency>
				<groupId>org.glassfish.jaxb</groupId>
				<artifactId>jaxb-runtime</artifactId>
				<scope>runtime</scope>
				<version>${jaxb.runtime.version}</version>
			</dependency>
			<dependency>
				<groupId>xerces</groupId>
				<artifactId>xmlParserAPIs</artifactId>
				<version>2.2.1</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>jtidy</groupId>
				<artifactId>jtidy</artifactId>
				<!-- make sure the version matches the one used by the httpunit -->
				<version>4aug2000r7-dev</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>javax.servlet</groupId>
				<artifactId>servlet-api</artifactId>
				<!-- make sure the version matches the one used by the httpunit -->
				<version>2.3</version>
				<scope>provided</scope>
			</dependency>
			<!-- Postgres JDBC driver -->
			<dependency>
				<groupId>org.postgresql</groupId>
				<artifactId>postgresql</artifactId>
				<version>42.7.5</version>
				<scope>runtime</scope>
			</dependency>
			<!-- Oracle JDBC driver -->
			<dependency>
				<groupId>oracle.ojdbc14</groupId>
				<artifactId>ojdbc14</artifactId>
				<version>10g</version>
			</dependency>
			<dependency>
				<groupId>antlr</groupId>
				<artifactId>antlr</artifactId>
				<version>2.7.6</version>
				<scope>runtime</scope>
			</dependency>
	       <dependency>
	           <groupId>com.sun.jersey.contribs</groupId>
	           <artifactId>jersey-spring</artifactId>
	           <version>1.19.3</version>
	           <scope>runtime</scope>
	           <exclusions>
	               <exclusion>
	                   <groupId>org.springframework</groupId>
	                   <artifactId>spring</artifactId>
	               </exclusion>
	               <exclusion>
	                   <groupId>org.springframework</groupId>
	                   <artifactId>spring-core</artifactId>
	               </exclusion>
	               <exclusion>
	                   <groupId>org.springframework</groupId>
	                   <artifactId>spring-web</artifactId>
	               </exclusion>
	               <exclusion>
	                   <groupId>org.springframework</groupId>
	                   <artifactId>spring-beans</artifactId>
	               </exclusion>
	               <exclusion>
	                   <groupId>org.springframework</groupId>
	                   <artifactId>spring-context</artifactId>
	               </exclusion>
	               <exclusion>
	                   <groupId>org.springframework</groupId>
	                   <artifactId>spring-aop</artifactId>
	               </exclusion>
	               <exclusion>
	                   <groupId>org.springframework</groupId>
	                   <artifactId>spring-asm</artifactId>
	               </exclusion>
	           </exclusions>
	       </dependency>
			<dependency>
				<groupId>com.lowagie</groupId>
				<artifactId>itext</artifactId>
				<version>2.1.2</version>
			</dependency>
			<dependency>
				<groupId>com.mockrunner.jdk15.jee5</groupId>
				<artifactId>mockrunner</artifactId>
				<version>0.4.2</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>com.mockrunner.jdk15.jee5</groupId>
				<artifactId>mockrunner-servlet</artifactId>
				<version>0.4.2</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>httpunit</groupId>
				<artifactId>httpunit</artifactId>
				<version>1.6.1</version>
				<scope>test</scope>
			</dependency>
			<!-- XMLUnit 2.x: canonical XML comparison for Phase B.0 Castor
			     characterisation tests. See DR-006 + phase-b-execution-playbook.md
			     §B.0. 2.10.x is the final 2.x release line on JDK 8. -->
			<dependency>
				<groupId>org.xmlunit</groupId>
				<artifactId>xmlunit-core</artifactId>
				<version>2.10.0</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.xmlunit</groupId>
				<artifactId>xmlunit-matchers</artifactId>
				<version>2.10.0</version>
				<scope>test</scope>
			</dependency>
			<dependency>
				<groupId>org.jdom</groupId>
				<artifactId>jdom</artifactId>
				<version>1.1</version>
			</dependency>
			<dependency>
				<groupId>taglibs</groupId>
				<artifactId>standard</artifactId>
				<version>1.1.2</version>
				<scope>runtime</scope>
			</dependency>
			<dependency>
				<groupId>org.apache.poi</groupId>
				<artifactId>poi</artifactId>
				<version>3.0.1-FINAL</version>
				<scope>compile</scope>
				<exclusions>
					<exclusion>
						<artifactId>log4j</artifactId>
						<groupId>log4j</groupId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>net.sourceforge.jexcelapi</groupId>
				<artifactId>jxl</artifactId>
				<version>2.6.10</version>
				<scope>compile</scope>
				<exclusions>
					<exclusion>
						<artifactId>log4j</artifactId>
						<groupId>log4j</groupId>
					</exclusion>
				</exclusions>
			</dependency>
			<!-- Struts web framework -->
			<dependency>
				<groupId>struts</groupId>
				<artifactId>struts</artifactId>
				<version>1.2.4</version>
				<scope>test</scope>
				<!-- @pgawade 16-NOV-2010 Excluded the antlr library from struts as we need to use that from hibernate -->
				<exclusions>
					<exclusion>
						<artifactId>antlr</artifactId>
						<groupId>antlr</groupId>
					</exclusion>
				</exclusions>
			</dependency>
			<!-- XML serializer-->
	    	<dependency>
	            <groupId>net.sf.json-lib</groupId>
	            <artifactId>json-lib</artifactId>
	            <version>2.4</version>
	            <classifier>jdk15</classifier>
	        </dependency>
	        <dependency>
	            <groupId>com.sun.jersey</groupId>
	            <artifactId>jersey-server</artifactId>
	            <version>1.19.3</version>
	        </dependency>
	        <!-- Phase E.5 #5: Mockito version managed by spring-boot-dependencies
	             BOM (5.17.0 at Boot 3.5.0). Explicit 1.9.5 dependencyManagement
	             pin removed; the legacy MockitoJUnitRunner package moved
	             org.mockito.runners -> org.mockito.junit in Mockito 2+, see
	             the 4 migrated tests under core/src/test for the import-path
	             change. -->
			<!-- Needed by commons-validator -->
	        <dependency>
	        	<groupId>oro</groupId>
	        	<artifactId>oro</artifactId>
	        	<version>2.0.8</version>
	        </dependency>
			<!-- CVE security overrides: force safe versions of transitive dependencies -->
			<dependency>
				<groupId>org.apache.santuario</groupId>
				<artifactId>xmlsec</artifactId>
				<version>2.1.7</version>
			</dependency>
			<dependency>
				<groupId>xalan</groupId>
				<artifactId>xalan</artifactId>
				<version>2.7.3</version>
			</dependency>
			<!--
			    xalan 2.7.x splits the formerly-monolithic xalan.jar into
			    xalan:xalan + xalan:serializer. batik-bridge:1.7 only pulls
			    xalan:xalan transitively; without an explicit serializer
			    pin, callers that touch org.apache.xml.serializer.* (e.g.
			    Castor's CastorRulesContainerCharacterisationTest) blow up
			    with NoClassDefFoundError on SerializerTrace. Pinning here
			    keeps both JARs at the same CVE-patched version.
			-->
			<dependency>
				<groupId>xalan</groupId>
				<artifactId>serializer</artifactId>
				<version>2.7.3</version>
			</dependency>
			<dependency>
				<groupId>com.mchange</groupId>
				<artifactId>mchange-commons-java</artifactId>
				<version>0.4.0</version>
			</dependency>
			<dependency>
				<groupId>xerces</groupId>
				<artifactId>xercesImpl</artifactId>
				<version>2.12.2</version>
			</dependency>
			<dependency>
				<groupId>com.mchange</groupId>
				<artifactId>c3p0</artifactId>
				<version>0.12.0</version>
			</dependency>
			<dependency>
				<groupId>org.yaml</groupId>
				<artifactId>snakeyaml</artifactId>
				<version>2.0</version>
			</dependency>
			<!-- Phase E.5 B3: springdoc-openapi exposes /v3/api-docs at
			     runtime. The SPA's `pnpm run codegen:openapi` step fetches
			     that endpoint to generate src/types/api.ts so TS types
			     stop drifting from backend DTO records. -->
			<dependency>
				<groupId>org.springdoc</groupId>
				<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
				<version>2.6.0</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<!-- These dependencies are used by _all_ child projects -->
	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.13.2</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.dbunit</groupId>
			<artifactId>dbunit</artifactId>
			<version>2.4.9</version>
			<scope>test</scope>
			<exclusions>
				<exclusion>
					<groupId>org.slf4j</groupId>
					<artifactId>slf4j-nop</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.slf4j</groupId>
					<artifactId>slf4j-api</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<!-- LOGGING -->
      <!--
		<dependency>
			<groupId>org.liquibase</groupId>
			<artifactId>liquibase-core</artifactId>
			<scope>runtime</scope>
		</dependency>
      -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${slf4j.version}</version>
		</dependency>
		<!--@pgawade 02-DEC-2010 Added jul-to-slf4j-1.5.6.jar to get liquibase
			logs using SLF4JBridgeHandler -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>jul-to-slf4j</artifactId>
			<version>${slf4j.version}</version>
			<scope>runtime</scope>
		</dependency>
		<!-- potentially replaces jul-to-slf4j
		<dependency>
    		<groupId>com.mattbertolini</groupId>
    		<artifactId>liquibase-slf4j</artifactId>
		</dependency>
		-->
		<!-- We have added this to see logging from SpringFramework it uses JCL -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>jcl-over-slf4j</artifactId>
			<version>${slf4j.version}</version>
			<scope>runtime</scope>
		</dependency>
		<!-- SPRING -->
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-tx</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-jdbc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-core</artifactId>
		</dependency>
		<dependency>
    		<groupId>org.springframework</groupId>
    		<artifactId>spring-context-support</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-oxm</artifactId>
			<version>${spring.version}</version>
		</dependency>
		<!-- Phase B.4 cliff: spring-security-oauth 2.3.5 dropped — see core/pom.xml. -->

		<!-- HIBERNATE -->
      <!--
		<dependency>
			<groupId>javax.validation</groupId>
			<artifactId>validation-api</artifactId>
			<version>2.0.1.Final</version>
		</dependency>
      -->
      <!--
		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-validator</artifactId>
			<version>5.4.2.Final</version>
		</dependency>
		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-core</artifactId>
			<version>5.4.2.Final</version>
		</dependency>
      -->
		<!-- LIQUIBASE -->
      <!--
		<dependency>
			<groupId>org.liquibase</groupId>
			<artifactId>liquibase-core</artifactId>
			<version>3.6.3</version>
		</dependency>
      -->
		<!-- Explicitly defining dependencies versions for Maven 3 -->
		<dependency>
			<groupId>antlr</groupId>
			<artifactId>antlr</artifactId>
			<version>2.7.6</version>
		</dependency>
      <!--
		<dependency>
			<groupId>commons-beanutils</groupId>
			<artifactId>commons-beanutils-core</artifactId>
			<version>1.8.3</version>
		</dependency>
      -->
      <!--
		<dependency>
			<groupId>io.dropwizard.metrics</groupId>
			<artifactId>metrics-core</artifactId>
			<version>3.1.2</version>
		</dependency>
		<dependency>
			<groupId>io.dropwizard.metrics</groupId>
			<artifactId>metrics-healthchecks</artifactId>
			<version>3.1.0</version>
		</dependency>
		<dependency>
			<groupId>io.dropwizard.metrics</groupId>
			<artifactId>metrics-jvm</artifactId>
			<version>3.1.2</version>
		</dependency>
      -->
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpclient</artifactId>
			<version>4.5.7</version>
		</dependency>
	</dependencies>
	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.13.0</version>
					<configuration>
						<source>21</source>
						<target>21</target>
						<compilerArgument>-Xlint:all</compilerArgument>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-resources-plugin</artifactId>
					<version>2.5</version>
					<configuration>
						<delimiters>
							<delimiter>${*}</delimiter>
						</delimiters>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<!-- Surefire 3.x natively integrates JUnit Platform; JDK 21 baseline
					     (Phase B.1) makes the 2.x lock-in obsolete. -->
					<version>3.5.3</version>
					<configuration>
						<skipTests>${skipTests}</skipTests>
						<!-- @{argLine} is resolved late so the JaCoCo prepare-agent goal
						     (bound to initialize) has already populated it.
						     Phase B.1 JDK 21 baseline: legacy Mockito + Spring/Hibernate
						     reflection needs java.base opens (InaccessibleObjectException
						     on ClassLoader.defineClass), and Castor 1.4.1's BaseXercesJDK5Serializer
						     touches an internal JDK class that needs java.xml exports. Both
						     flags are stopgaps that go away when B.3 swaps Castor for Jakarta
						     JAXB and Mockito is eventually bumped (see MIGRATION.md). -->
						<argLine>@{argLine} --add-opens=java.base/java.lang=ALL-UNNAMED --add-opens=java.base/java.lang.invoke=ALL-UNNAMED --add-opens=java.base/java.lang.reflect=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED --add-exports=java.xml/com.sun.org.apache.xml.internal.serialize=ALL-UNNAMED</argLine>
						<!-- Excluded from default `mvn test`:
						       - DAO tests extending HibernateOcDbTestCase / OcDbTestCase: require
						         a live PostgreSQL on localhost:5432; static initializer fails
						         without a DB and triggers NoClassDefFoundError on the test class.
						       - RulesPostImportContainerServiceTest: only test method is commented
						         out; JUnit reports "No tests found in...". Broken-by-omission.
						     These remain in src/test/java to preserve git history and will be
						     re-enabled as Testcontainers-based integration tests in a later
						     Phase 0 iteration. See MIGRATION.md § Phase 0. -->
						<excludes>
							<exclude>**/dao/**/*DaoTest.java</exclude>
							<exclude>**/service/rule/RuleSetServiceTest.java</exclude>
							<exclude>**/service/rule/expression/SampleTest.java</exclude>
							<exclude>**/service/rule/RulesPostImportContainerServiceTest.java</exclude>
							<!-- Phase B.0 Castor characterisation tests extend
							     HibernateOcDbTestCase: a live Postgres is required.
							     The framework smoke test
							     (CastorCharacterisationFrameworkTest) is exempt:
							     it runs in the default suite and verifies the
							     XMLUnit dependency is wired correctly. -->
							<exclude>**/odm/characterisation/*IT.java</exclude>
							<!-- Phase 0 integration-test backlog: critical-path
							     ITs (LoginFlowIT, StudyCrudIT, …) live in the
							     dedicated `it/` package, extend
							     HibernateOcDbTestCase, and require a live
							     Postgres. Run via the integration-tests profile. -->
							<exclude>**/it/*IT.java</exclude>
						</excludes>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-release-plugin</artifactId>
					<version>2.3</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-assembly-plugin</artifactId>
					<version>2.2.2</version>
					<configuration>
						<outputDirectory>${openclinica.distros.dir}</outputDirectory>
						<descriptors>
							<descriptor>${project.basedir}/src/assemble/openclinica-assemble.xml</descriptor>
						</descriptors>
						<appendAssemblyId>false</appendAssemblyId>
						<finalName>${assembly.name}-${project.version}</finalName>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-eclipse-plugin</artifactId>
					<version>2.8</version>
					<configuration>
						<addVersionToProjectName>true</addVersionToProjectName>
					</configuration>
				</plugin>
				<!-- JaCoCo: line-coverage instrumentation + HTML/XML reports.
				     prepare-agent populates the `argLine` Maven property (referenced
				     by surefire `@{argLine}`); the `report` execution generates
				     core/target/site/jacoco/ at the end of the `test` phase. No
				     coverage threshold enforced yet (Phase 0 goal: track baseline). -->
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>0.8.12</version>
					<executions>
						<execution>
							<id>jacoco-prepare-agent</id>
							<goals>
								<goal>prepare-agent</goal>
							</goals>
						</execution>
						<execution>
							<id>jacoco-report</id>
							<phase>test</phase>
							<goals>
								<goal>report</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
				<!-- OWASP dependency-check: scans Maven dependencies against the NVD
				     for known CVEs. Not bound to a build phase (slow first run +
				     downloads CVE database). Invoke explicitly:
				         mvn org.owasp:dependency-check-maven:check
				     or via the .github/workflows/security.yml nightly job. Pinning
				     to 9.x avoids the 10.x NVD-API-key hard requirement. -->
				<plugin>
					<groupId>org.owasp</groupId>
					<artifactId>dependency-check-maven</artifactId>
					<version>9.2.0</version>
					<configuration>
						<failBuildOnCVSS>8</failBuildOnCVSS>
						<skipTestScope>true</skipTestScope>
						<formats>
							<format>HTML</format>
							<format>SARIF</format>
							<format>JSON</format>
						</formats>
						<suppressionFiles>
							<suppressionFile>${project.basedir}/dependency-check-suppressions.xml</suppressionFile>
						</suppressionFiles>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.codehaus.cargo</groupId>
					<artifactId>cargo-maven2-plugin</artifactId>
					<version>1.1.4</version>
					<configuration>
						<!-- Container configuration -->
						<container>
							<containerId>tomcat6x</containerId>
							<home>${catalina.home}</home>
						</container>
						<configuration>
							<home>${catalina.home}</home>
							<type>existing</type>
						</configuration>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.liquibase</groupId>
					<artifactId>liquibase-plugin</artifactId>
					<version>1.9.1.0</version>
					<configuration>
						<propertyFile>${config.file}</propertyFile>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.zeroturnaround</groupId>
					<artifactId>jrebel-maven-plugin</artifactId>
					<version>1.0.7</version>
					<executions>
						<execution>
							<id>generate-rebel-xml</id>
							<phase>process-resources</phase>
							<goals>
								<goal>generate</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>buildnumber-maven-plugin</artifactId>
					<version>1.3</version>
					<executions>
						<execution>
							<id>buildnumber</id>
							<phase>validate</phase>
							<goals>
								<goal>create</goal>
							</goals>
							<configuration>
								<doCheck>false</doCheck>
								<doUpdate>false</doUpdate>
								<shortRevisionLength>12</shortRevisionLength>
								<buildNumberPropertyName>changeSet</buildNumberPropertyName>
							</configuration>
						</execution>
						<execution>
							<id>timestamp</id>
							<phase>validate</phase>
							<goals>
								<goal>create-timestamp</goal>
							</goals>
							<configuration>
								<timestampPropertyName>changeSetDate</timestampPropertyName>
								<timestampFormat>yyyy-MM-dd HH:mm Z</timestampFormat>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>org.jvnet.jaxb2.maven2</groupId>
					<artifactId>maven-jaxb2-plugin</artifactId>
					<version>0.7.5</version>
					<executions>
						<execution>
							<phase>generate-sources</phase>
							<goals>
								<goal>generate</goal>
							</goals>
						</execution>
					</executions>
					<configuration>
						<schemaDirectory>src/main/webapp/WEB-INF/schemas</schemaDirectory>
					</configuration>
				</plugin>
				<plugin>
			        <groupId>org.apache.maven.plugins</groupId>
			        <artifactId>maven-dependency-plugin</artifactId>
			        <!-- 3.1.2 → 3.8.1: 3.1.2 bundles an ASM that can't read class file
			             major version 65 (JDK 21). 3.8.1 ships ASM 9.7. Phase B.1 gate. -->
			        <version>3.8.1</version>
			        <executions>
			        	<execution>
			        		<id>analyze</id>
			        		<phase>package</phase>
			        		<goals>
			        			<goal>analyze-only</goal>
			        		</goals>
			        	</execution>
			        </executions>
			        <configuration>
			        	<failOnWarning>true</failOnWarning>
			        	<ignoreNonCompile>true</ignoreNonCompile>
						<ignoredUsedUndeclaredDependencies>
							<ignoredUsedUndeclaredDependency>stax:stax-api</ignoredUsedUndeclaredDependency>
							<!-- Phase B.4 smoke harness (PR #37 + #38): SmokeIT imports from
							     selenium-api / -chrome-driver / -chromium-driver / -remote-driver /
							     -support, all transitives of the umbrella selenium-java dep
							     declared in web/pom.xml. Listing each separately would be
							     repetitive + drift-prone. -->
							<ignoredUsedUndeclaredDependency>org.seleniumhq.selenium:selenium-api</ignoredUsedUndeclaredDependency>
							<ignoredUsedUndeclaredDependency>org.seleniumhq.selenium:selenium-chrome-driver</ignoredUsedUndeclaredDependency>
							<ignoredUsedUndeclaredDependency>org.seleniumhq.selenium:selenium-chromium-driver</ignoredUsedUndeclaredDependency>
							<ignoredUsedUndeclaredDependency>org.seleniumhq.selenium:selenium-remote-driver</ignoredUsedUndeclaredDependency>
							<ignoredUsedUndeclaredDependency>org.seleniumhq.selenium:selenium-support</ignoredUsedUndeclaredDependency>
							<!-- Phase C.1: Boot's BOM ships angus-mail as the jakarta.mail
							     impl. Existing email code references jakarta.mail types via
							     jakarta.mail-api; analyze-only sees the impl on classpath
							     and flags it as used-undeclared. -->
							<ignoredUsedUndeclaredDependency>org.eclipse.angus:jakarta.mail</ignoredUsedUndeclaredDependency>
						</ignoredUsedUndeclaredDependencies>
						<ignoredUnusedDeclaredDependencies>
							<!-- Needed by commons-validator -->
							<ignoredUnusedDeclaredDependency>oro:oro</ignoredUnusedDeclaredDependency>
							<!-- Phase B.4 cliff: Castor was retired in B.3 — the path that
							     used xalan.serializer is gone; the dep is still pulled in
							     transitively by fop / batik. commons-validator is web-only
							     but appears in core's transitive classpath. -->
							<ignoredUnusedDeclaredDependency>commons-validator:commons-validator</ignoredUnusedDeclaredDependency>
							<ignoredUnusedDeclaredDependency>xalan:serializer</ignoredUnusedDeclaredDependency>
							<!-- Phase C.1: Spring Boot starters added for the C.2-C.16
							     sub-phases. No direct compile-time references yet (no
							     @SpringBootApplication until C.2); the C-phase
							     conversions will start using them. -->
							<ignoredUnusedDeclaredDependency>org.springframework.boot:spring-boot-starter</ignoredUnusedDeclaredDependency>
							<ignoredUnusedDeclaredDependency>org.springframework.boot:spring-boot-starter-web</ignoredUnusedDeclaredDependency>
							<ignoredUnusedDeclaredDependency>org.springframework.boot:spring-boot-starter-data-jpa</ignoredUnusedDeclaredDependency>
							<ignoredUnusedDeclaredDependency>org.springframework.boot:spring-boot-starter-mail</ignoredUnusedDeclaredDependency>
							<ignoredUnusedDeclaredDependency>org.springframework.boot:spring-boot-starter-quartz</ignoredUnusedDeclaredDependency>
							<ignoredUnusedDeclaredDependency>org.springframework.boot:spring-boot-starter-validation</ignoredUnusedDeclaredDependency>
							<!-- Boot BOM upgrades jakarta.mail-api to a version where the
							     impl ships in org.eclipse.angus:jakarta.mail. The legacy
							     api-only dep is still declared explicitly; the angus impl
							     comes via the Boot BOM transitively. -->
							<ignoredUnusedDeclaredDependency>jakarta.mail:jakarta.mail-api</ignoredUnusedDeclaredDependency>
						</ignoredUnusedDeclaredDependencies>
			        </configuration>
			      </plugin>
               <plugin>
                  <groupId>org.apache.maven.plugins</groupId>
                  <artifactId>maven-enforcer-plugin</artifactId>
                  <version>3.0.0</version>
                  <executions>
                     <execution>
                        <id>enforce-ban-duplicate-classes</id>
                        <goals>
                           <goal>enforce</goal>
                        </goals>
                        <configuration>
                           <rules>
                              <banDuplicateClasses>
								  <ignoreClasses/>
                                 <scopes>
                                    <scope>compile</scope>
                                    <scope>provided</scope>
                                 </scopes>
                                 <findAllDuplicates>false</findAllDuplicates>
                                 <ignoreWhenIdentical>false</ignoreWhenIdentical>
                              </banDuplicateClasses>
                           </rules>
                           <fail>true</fail>
                        </configuration>
                     </execution>
                  </executions>
                  <dependencies>
                     <dependency>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>extra-enforcer-rules</artifactId>
                        <version>1.3</version>
                     </dependency>
                  </dependencies>
               </plugin>
			</plugins>
		</pluginManagement>

		<!-- Plugins activated for every module (configuration inherited from
		     pluginManagement above). JaCoCo runs on every test execution so
		     every module produces a coverage report. -->
		<plugins>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
			</plugin>
		</plugins>

		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>**/*.properties</include>
					<include>**/*.xml</include>
				</includes>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
				<excludes>
					<exclude>**/*.properties</exclude>
					<exclude>**/*.xml</exclude>
				</excludes>
			</resource>
		</resources>


		<testResources>
			<testResource>
				<directory>src/test/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>**/*.properties</include>
					<include>**/*.xml</include>
				</includes>
			</testResource>
		</testResources>

		<!--
		<filters>
			<filter>src/main/filters/default.properties</filter>
			<filter>src/main/filters/${config.id}.properties</filter>
		</filters>
		-->

	</build>
	<scm>
		<connection>scm:git:${git.url}</connection>
		<developerConnection>scm:git:${git.url}</developerConnection>
		<tag>HEAD</tag>
	</scm>

	<repositories>
		<repository>
			<!-- Phase B.4: openclinica jfrog now returns 401 for unknown
			     artifacts (jakarta.* deps weren't there in the javax era).
			     updatePolicy=never lets Maven keep using the cached jars
			     (jmesa-2.4.2-oc, log4jdbc4-1.2) without re-checking the
			     401-walled repo on every build. -->
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
			<releases>
				<enabled>true</enabled>
				<updatePolicy>never</updatePolicy>
				<checksumPolicy>warn</checksumPolicy>
			</releases>
			<id>openclinica.jfrog.io-libs-release</id>
			<name>openclinica.jfrog.io-libs-release</name>
			<url>https://openclinica.jfrog.io/openclinica/libs-release</url>
		</repository>
		<repository>
			<id>jboss-public-repository-group</id>
			<name>JBoss Public Repository Group</name>
			<url>https://repository.jboss.org/nexus/content/groups/public/</url>
			<layout>default</layout>
			<releases>
				<enabled>true</enabled>
				<updatePolicy>never</updatePolicy>
			</releases>
			<snapshots>
				<enabled>true</enabled>
				<updatePolicy>never</updatePolicy>
			</snapshots>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>jboss-public-repository-group</id>
			<name>JBoss Public Repository Group</name>
			<url>https://repository.jboss.org/nexus/content/groups/public/</url>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
		</pluginRepository>
	</pluginRepositories>

	<distributionManagement>
		<repository>
			<id>openclinica-artifactory</id>
			<name>dev.openclinica.com-releases</name>
			<url>http://dev.openclinica.com/artifactory/libs-release-local</url>
		</repository>
		<snapshotRepository>
			<id>openclinica-artifactory</id>
			<name>dev.openclinica.com-snapshots</name>
			<url>http://dev.openclinica.com/artifactory/libs-snapshot-local</url>
		</snapshotRepository>
	</distributionManagement>

</project>
